specVersion: "v1beta"
serviceId: "basketball-stat-tracker"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "basketball-stat-tracker-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
